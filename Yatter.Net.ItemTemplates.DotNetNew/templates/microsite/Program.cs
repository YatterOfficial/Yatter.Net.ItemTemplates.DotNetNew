using System;

namespace Yatter.Template
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Hello Yatter!");

            /* Create any automation features here. 
             * 
             * Or use the command line:
             * 
             *  -   You can pack this into a lightweight archive (*.yatra)
             *      for uploading using the following CLI command:

                yatter microsite -p -r null -a -f "Archive.yatra"

            *   n.b. You will need to have already installed yatter using the following command:

                dotnet tool install --global Yatter.Net.Tools.CLI

            *   See https://www.nuget.org/packages/Yatter.Net.Tools.CLI/
            *   and https://github.com/YatterOfficial/Yatter.Net.Tools.CLI
            */

        }
    }
}